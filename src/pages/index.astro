---
// src/pages/index.astro
import BaseLayout from '../layouts/Base.astro';

// 可选：如果你不需要顶部导航链接到 /blog 或 /about，可以删掉 BaseLayout
// 但建议保留，以便未来扩展
---

<BaseLayout title="AirGlow - 点亮你的空间" description="智能氛围灯，用色彩定义你的心情。">
  <style>
    /* ============= 全局重置与基础样式 ============= */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      background: #fff;
      color: #1d1d1f;
      line-height: 1.5;
    }

    /* 深色模式支持（系统自动）*/
    @media (prefers-color-scheme: dark) {
      body {
        background: #000;
        color: #f5f5f7;
      }
      .hero::before {
        background: rgba(0, 0, 0, 0.6);
      }
    }

    /* ============= 导航栏 ============= */
    nav.main-nav {
      display: flex;
      justify-content: center;
      padding: 18px 0;
      font-size: 14px;
    }
    nav.main-nav a {
      margin: 0 20px;
      text-decoration: none;
      color: inherit;
      opacity: 0.8;
      transition: opacity 0.3s;
    }
    nav.main-nav a:hover {
      opacity: 1;
    }

    /* ============= 英雄区域 ============= */
    .hero {
      position: relative;
      height: 90vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 0 20px;
    }
    .hero h1 {
      font-size: 56px;
      font-weight: 600;
      letter-spacing: -0.01em;
      margin-bottom: 12px;
    }
    .hero p {
      font-size: 24px;
      max-width: 600px;
      margin-bottom: 30px;
      opacity: 0.8;
    }
    .cta-button {
      background: #0071e3;
      color: white;
      border: none;
      border-radius: 980px;
      padding: 12px 30px;
      font-size: 20px;
      font-weight: 500;
      cursor: pointer;
      transition: background 0.3s;
    }
    .cta-button:hover {
      background: #0077ed;
    }

    /* ============= 产品特性区 ============= */
    .features {
      padding: 80px 20px;
      max-width: 1000px;
      margin: 0 auto;
    }
    .feature {
      display: flex;
      margin-bottom: 70px;
      align-items: center;
      gap: 40px;
    }
    .feature.reverse {
      flex-direction: row-reverse;
    }
    .feature img {
      width: 45%;
      border-radius: 24px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.2);
    }
    .feature-text h2 {
      font-size: 36px;
      font-weight: 600;
      margin-bottom: 16px;
    }
    .feature-text p {
      font-size: 20px;
      opacity: 0.8;
    }

    /* ============= 页脚 ============= */
    footer.custom-footer {
      text-align: center;
      padding: 40px 0 60px;
      font-size: 12px;
      opacity: 0.6;
    }

    /* ============= 响应式 ============= */
    @media (max-width: 768px) {
      .hero h1 { font-size: 40px; }
      .hero p { font-size: 18px; }
      .feature {
        flex-direction: column !important;
        text-align: center;
      }
      .feature img {
        width: 90%;
      }
    }
  </style>

  <!-- 导航 -->
  <nav class="main-nav">
    <a href="/">AirGlow</a>
    <a href="#">功能</a>
    <a href="#">技术规格</a>
    <a href="#">购买</a>
  </nav>

  <!-- 英雄区域 -->
  <section class="hero">
    <h1>光，随你所想。</h1>
    <p>AirGlow 智能氛围灯，用色彩定义你的心情。</p>
    <button class="cta-button">立即购买</button>
  </section>

  <!-- 特性展示 -->
  <section class="features">
    <div class="feature">
      <img src="https://placehold.co/600x400/e0e0e0/1d1d1f?text=AirGlow+Front" alt="AirGlow 正面">
      <div class="feature-text">
        <h2>极简设计</h2>
        <p>一体成型铝合金机身，仅 1.2cm 厚，融入任何空间。</p>
      </div>
    </div>

    <div class="feature reverse">
      <img src="https://placehold.co/600x400/1d1d1f/f5f5f7?text=Colors+Demo" alt="色彩演示">
      <div class="feature-text">
        <h2>1600 万色随心换</h2>
        <p>通过 App 或语音控制，瞬间切换氛围，支持日出唤醒与睡眠模式。</p>
      </div>
    </div>
  </section>

  <!-- 页脚 -->
  <footer class="custom-footer">
    © 2025 AirGlow Inc. 所有权利保留。本产品为虚构演示。
  </footer>
</BaseLayout>
